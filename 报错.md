
## 🛠️ 怎么和 Claude 说明并解决

为了让 Claude 瞬间精准定位，你可以直接把**报错截图**、**前端请求代码**以及**你的网络配置**一起喂给它。你可以直接复制下面这段话发给 Claude：

> **💡 复制给 Claude 的提示词：**
> 我在运行项目时前端报错：`SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON`，同时控制台显示 `Failed to load resource: the server responded with a status of 404 (Not Found)`。
> 我知道这是因为前端期望接收 JSON，但后端/服务器却返回了 HTML（比如 404 页面或前端 index.html）。
> **以下是我的相关代码和配置：**
> 1. **出错的源码位置（`weekly-report.html` 第 51 行附近）：**
> ```javascript
    async function loadWeeklyReport() {
      try {
        const res = await APP.api.get('/reports/weekly');
        if (res && res.code === 200) {
          renderReport(res.data);
        } else {
          renderError(res?.message || '加载失败');
        }
      } catch (err) {
        console.error('加载周报失败:', err);
        renderError(err.message || '网络异常，请稍后重试');
      }
    }
>
> ```
>
>
> 2. **我的前端代理配置（如果是 Vue3/Vite 项目，贴出 `vite.config.js` 里的 `proxy`）：**
> ```javascript
> // 如果有配置文件，贴在这里（注意检查你的本地代理端口，比如是不是 7892 之类的代理影响了流量）
>
> ```
>
>
> 3. **我的后端环境：**
>  运行在 localhost:8888，
>
>
> 请帮我检查：
> 1. 为什么这个请求会报 404？
> 2. 我的请求路径（URL）或者前端 Proxy 配置哪里写错了？
> 3. 应该如何修改前端的请求代码和代理配置？
>
>
