我们要为系统增加最核心的亮点功能：简易版“AI 饮食智能分析助理”。

实现逻辑如下：
1. 后端新增接口：在后端写一个 `POST /api/ai/analyze` 接口。该接口接收用户输入的文本（如 "我早餐吃了2个包子和200ml纯牛奶"）或食物图片的 URL。
2. 后端对接大模型：在后端使用 fetch/axios 调用外部大模型 API（优先推荐使用 DeepSeek请帮我编写通用的 API 请求骨架，并在环境变量 .env 中留出 API_KEY 变量位置）。
3. 设计 Prompt（提示词）：让大模型必须以严格的 JSON 数组格式返回识别结果，不得包含任何 Markdown 语法或多余的解释。格式要求如下：
   [
     { "foodName": "包子(肉馅)", "weight": 200 },
     { "foodName": "牛奶", "weight": 200 }
   ]
4. 后端数据库匹配：后端拿到大模型输出的数组后，去 `food_items` 表中根据 `foodName` 进行模糊查询（LIKE），查出对应的 `food_id`。如果查到了，将数据组装好，直接在后端内部重定向调用或复用之前写好的 `/api/mealRecord/batch` 逻辑，批量存入数据库！

5. 前台客户端修改：
   - 在前端首页的“快捷操作”或“饮食记录”区域，加入一个“AI 智能录入”按钮。
   - 点击后弹出一个轻量级的对话框（Modal），里面包含：一个用餐时段选择器（早餐/午餐/晚餐）、一个输入大白话的文本框（textarea）、一个输入图片 URL 的输入框。
   - 点击“AI 智能分析”按钮后，按钮变成 Loading 状态。请求成功后，关闭弹窗，并温柔地提示用户：“AI 已成功为您识别并录入：包子、牛奶！”。同时刷新今日热量进度条。

请帮我找出对应的后端路由文件、并修改前端页面及样式，把这个全栈闭环做出来。