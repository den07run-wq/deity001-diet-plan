我发现了一个公告功能的闭环问题：目前前台首页的公告横幅（📢）只会展示最新发布的一条公告，发布新公告后，以前的旧公告就查询不到了。

我需要实现一个查看历史公告的功能，请帮我按照以下步骤打通全栈链路：

1. 后端新增/检查接口：确保公开的公告路由（如 `backend/routes/announcement.js`）里有一个可以获取所有“已发布”状态公告的列表接口（例如 `GET /api/announcements/list`），支持按发布时间倒序排列。
2. 前台添加“查看历史”入口：
   - 在前台首页公告横幅的右侧（“最新公告：xxxx”的尾部），加上一个带下划线的点击链接或小按钮，文字叫“查看历史公告”。
3. 前台弹窗设计（Modal）：
   - 在 `index.html` 底部写一个隐藏的历史公告弹窗组件。
   - 当用户点击“查看历史公告”时，触发 JS 请求后端的列表接口，并将所有的历史公告（标题、内容、发布时间）用一个干净的列表形式（例如 Element 风格的轻量级时间线 Timeline 或者是带有分割线的 ul 列表）展示在弹窗里。
4. 样式样式融合：
   - 弹窗的关闭按钮、列表滚动条等样式要符合前台现有的健康清新风格，点击弹窗外部或右上角叉叉可以关闭弹窗。

请帮我找出前台的 HTML、CSS、JS 文件以及后端的公告路由，把这个查看历史的闭环做出来。